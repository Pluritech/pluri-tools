{
  "name": "@pluritech/gmaps-utils",
  "version": "1.1.1",
  "description": "One class with methods to work with google places",
  "main": "index.ts",
  "scripts": {
    "buildOld": "./node_modules/.bin/rimraf ./dist && ./node_modules/.bin/tsc -p tsconfig.json && cp ./package.json ./dist",
    "build": "./node_modules/.bin/rimraf ./dist && mkdir dist && cp ./lib/* ./dist && cp ./package.json ./dist",
    "publishMe": "npm publish ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "angular",
    "ionic",
    "googlemaps",
    "maps",
    "places",
    "typescript"
  ],
  "author": "Lucas Corrêa",
  "license": "MIT",
  "devDependencies": {
    "@types/googlemaps": "^3.30.0",
    "rimraf": "^2.6.2",
    "typescript": "^2.6.1"
  }
}
